#pragma once

// Copy this file to credentials.h and fill in your values.
// credentials.h is gitignored — never commit real values.

// WiFi credentials
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASS "your_wifi_password"

// Spotify app credentials — register at https://developer.spotify.com/dashboard
#define SPOTIFY_CLIENT_ID     "your_spotify_client_id"
#define SPOTIFY_CLIENT_SECRET "your_spotify_client_secret"
